{
  "name": "github-actions-pm2-deploy-template",
  "version": "1.0.0",
  "description": "A production-ready template for deploying Node.js TypeScript applications to PM2 via self-hosted GitHub Actions runners.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.7"
  }
}
